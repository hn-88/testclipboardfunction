# testclipboardfunction
for testing a function to get clipboard contents on Linux, from CEF / Chrome.

This works when run from the terminal. But integration into OpenSpace probably needs some more work, like passing the Display of the OpenSpace process, and so on.
